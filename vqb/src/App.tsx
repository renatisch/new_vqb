import { FC } from "react";

import { QueryBuilderDialog } from "./components/Dialog";

export const App: FC = () => {
  return (
    <div className="App">
      <QueryBuilderDialog />
    </div>
  );
}
